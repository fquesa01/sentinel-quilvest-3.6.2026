<!DOCTYPE html>

<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sentinel - Start Screen Mockup</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600&family=Libre+Baskerville:wght@400;700&display=swap" rel="stylesheet">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

```
:root {
  --bg-deep: #0a0f0d;
  --bg-card: #141c18;
  --bg-card-hover: #1a2420;
  --border: #2a3830;
  --accent: #5b9a7c;
  --accent-glow: rgba(91, 154, 124, 0.15);
  --text-primary: #e8ede9;
  --text-secondary: #8a9b90;
  --text-muted: #5a6b60;
}

body {
  font-family: 'DM Sans', sans-serif;
  background: var(--bg-deep);
  min-height: 100vh;
  display: flex;
  justify-content: center;
  align-items: flex-start;
  padding: 40px 20px;
}

.phone-frame {
  width: 375px;
  height: 812px;
  background: var(--bg-deep);
  border-radius: 40px;
  border: 3px solid #2a2a2a;
  overflow: hidden;
  position: relative;
  box-shadow: 
    0 50px 100px rgba(0,0,0,0.5),
    inset 0 0 0 1px rgba(255,255,255,0.05);
}

.status-bar {
  height: 44px;
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 0 28px;
  font-size: 14px;
  font-weight: 600;
  color: var(--text-primary);
}

.status-bar-right {
  display: flex;
  gap: 6px;
  align-items: center;
}

.dynamic-island {
  width: 120px;
  height: 34px;
  background: #000;
  border-radius: 20px;
  position: absolute;
  top: 10px;
  left: 50%;
  transform: translateX(-50%);
}

.screen-content {
  padding: 20px 24px;
  height: calc(100% - 44px);
  display: flex;
  flex-direction: column;
}

.header {
  margin-bottom: 8px;
}

.logo {
  font-family: 'Libre Baskerville', serif;
  font-size: 22px;
  font-weight: 700;
  color: var(--text-primary);
  letter-spacing: -0.5px;
}

.logo-sub {
  font-family: 'DM Sans', sans-serif;
  font-size: 11px;
  color: var(--text-muted);
  letter-spacing: 1.5px;
  text-transform: uppercase;
  margin-top: 2px;
}

.hero {
  margin: 32px 0 40px;
}

.hero h1 {
  font-family: 'Libre Baskerville', serif;
  font-size: 28px;
  font-weight: 400;
  color: var(--text-primary);
  line-height: 1.3;
  letter-spacing: -0.5px;
}

.action-cards {
  display: flex;
  flex-direction: column;
  gap: 12px;
  flex: 1;
}

.action-card {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: 16px;
  padding: 20px;
  cursor: pointer;
  transition: all 0.2s ease;
  display: flex;
  align-items: flex-start;
  gap: 16px;
}

.action-card:hover {
  background: var(--bg-card-hover);
  border-color: var(--accent);
  transform: translateY(-2px);
  box-shadow: 0 8px 32px var(--accent-glow);
}

.action-icon {
  width: 44px;
  height: 44px;
  border-radius: 12px;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.action-icon svg {
  width: 22px;
  height: 22px;
}

.action-icon.navigate {
  background: linear-gradient(135deg, #1a3a2a 0%, #0f2018 100%);
}
.action-icon.navigate svg { stroke: #5b9a7c; }

.action-icon.record {
  background: linear-gradient(135deg, #2a1a1a 0%, #1a0f0f 100%);
}
.action-icon.record svg { stroke: #c47070; }

.action-icon.research {
  background: linear-gradient(135deg, #1a2a3a 0%, #0f1820 100%);
}
.action-icon.research svg { stroke: #7090c4; }

.action-content {
  flex: 1;
  min-width: 0;
}

.action-title {
  font-size: 16px;
  font-weight: 600;
  color: var(--text-primary);
  margin-bottom: 4px;
  display: flex;
  align-items: center;
  gap: 8px;
}

.action-title .badge {
  font-size: 9px;
  padding: 3px 6px;
  border-radius: 4px;
  font-weight: 500;
  letter-spacing: 0.5px;
}

.badge.ai {
  background: rgba(196, 112, 112, 0.15);
  color: #c47070;
}

.badge.privileged {
  background: rgba(112, 144, 196, 0.15);
  color: #7090c4;
}

.action-desc {
  font-size: 13px;
  color: var(--text-secondary);
  line-height: 1.4;
}

.action-arrow {
  color: var(--text-muted);
  flex-shrink: 0;
  align-self: center;
}

.quick-input {
  margin-top: auto;
  padding-top: 24px;
}

.input-wrapper {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: 14px;
  padding: 14px 16px;
  display: flex;
  align-items: center;
  gap: 12px;
}

.input-wrapper input {
  flex: 1;
  background: none;
  border: none;
  outline: none;
  font-size: 14px;
  color: var(--text-primary);
  font-family: 'DM Sans', sans-serif;
}

.input-wrapper input::placeholder {
  color: var(--text-muted);
}

.input-btn {
  width: 36px;
  height: 36px;
  border-radius: 10px;
  border: none;
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  transition: all 0.2s ease;
}

.input-btn.mic {
  background: transparent;
  color: var(--text-secondary);
}

.input-btn.send {
  background: var(--accent);
  color: var(--bg-deep);
}

.input-btn.send:hover {
  filter: brightness(1.1);
}

.home-indicator {
  width: 134px;
  height: 5px;
  background: var(--text-muted);
  border-radius: 3px;
  margin: 20px auto 8px;
}

/* Subtle ambient glow */
.screen-content::before {
  content: '';
  position: absolute;
  top: 100px;
  left: 50%;
  transform: translateX(-50%);
  width: 300px;
  height: 300px;
  background: radial-gradient(circle, var(--accent-glow) 0%, transparent 70%);
  pointer-events: none;
  opacity: 0.5;
}
```

  </style>
</head>
<body>
  <div class="phone-frame">
    <div class="dynamic-island"></div>
    <div class="status-bar">
      <span>7:17</span>
      <div class="status-bar-right">
        <svg width="18" height="12" viewBox="0 0 18 12" fill="currentColor">
          <rect x="0" y="3" width="3" height="9" rx="1"/>
          <rect x="5" y="2" width="3" height="10" rx="1"/>
          <rect x="10" y="0" width="3" height="12" rx="1"/>
          <rect x="15" y="0" width="3" height="12" rx="1" opacity="0.3"/>
        </svg>
        <span style="font-size: 12px;">5G</span>
        <svg width="25" height="12" viewBox="0 0 25 12" fill="currentColor">
          <rect x="0" y="1" width="21" height="10" rx="2" stroke="currentColor" stroke-width="1" fill="none"/>
          <rect x="2" y="3" width="16" height="6" rx="1" fill="#5b9a7c"/>
          <rect x="22" y="4" width="2" height="4" rx="0.5"/>
        </svg>
      </div>
    </div>

```
<div class="screen-content">
  <div class="header">
    <div class="logo">Sentinel</div>
    <div class="logo-sub">Counsel LLP</div>
  </div>

  <div class="hero">
    <h1>What do you need?</h1>
  </div>

  <div class="action-cards">
    <div class="action-card">
      <div class="action-icon navigate">
        <svg viewBox="0 0 24 24" fill="none" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <polygon points="3 11 22 2 13 21 11 13 3 11"/>
        </svg>
      </div>
      <div class="action-content">
        <div class="action-title">Navigate</div>
        <div class="action-desc">Jump to any matter, document, or feature</div>
      </div>
      <div class="action-arrow">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M9 18l6-6-6-6"/>
        </svg>
      </div>
    </div>

    <div class="action-card">
      <div class="action-icon record">
        <svg viewBox="0 0 24 24" fill="none" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M12 2a3 3 0 0 0-3 3v7a3 3 0 0 0 6 0V5a3 3 0 0 0-3-3Z"/>
          <path d="M19 10v2a7 7 0 0 1-14 0v-2"/>
          <line x1="12" x2="12" y1="19" y2="22"/>
        </svg>
      </div>
      <div class="action-content">
        <div class="action-title">Record <span class="badge ai">AI</span></div>
        <div class="action-desc">Docs surface as you speak</div>
      </div>
      <div class="action-arrow">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M9 18l6-6-6-6"/>
        </svg>
      </div>
    </div>

    <div class="action-card">
      <div class="action-icon research">
        <svg viewBox="0 0 24 24" fill="none" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <rect width="18" height="18" x="3" y="3" rx="2"/>
          <path d="M3 9h18"/>
          <path d="M9 21V9"/>
        </svg>
      </div>
      <div class="action-content">
        <div class="action-title">Research <span class="badge privileged">PRIVILEGED</span></div>
        <div class="action-desc">Search all data under privilege</div>
      </div>
      <div class="action-arrow">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M9 18l6-6-6-6"/>
        </svg>
      </div>
    </div>
  </div>

  <div class="quick-input">
    <div class="input-wrapper">
      <input type="text" placeholder="Ask Emma anything...">
      <button class="input-btn mic">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M12 2a3 3 0 0 0-3 3v7a3 3 0 0 0 6 0V5a3 3 0 0 0-3-3Z"/>
          <path d="M19 10v2a7 7 0 0 1-14 0v-2"/>
        </svg>
      </button>
      <button class="input-btn send">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
          <path d="M5 12h14M12 5l7 7-7 7"/>
        </svg>
      </button>
    </div>
  </div>

  <div class="home-indicator"></div>
</div>
```

  </div>
</body>
</html>